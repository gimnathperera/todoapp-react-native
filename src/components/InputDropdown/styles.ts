import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  inputDropdown: {
    borderRadius: 12,
    paddingHorizontal: 8,
    borderColor: "#3E2BCC",
    borderWidth: 1,
    width: "100%",
  },
});

export default styles;
