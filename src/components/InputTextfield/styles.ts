import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  textInput: {
    borderRadius: 12,
    paddingVertical: 14,
    paddingHorizontal: 8,
    borderColor: "#3E2BCC",
    borderWidth: 1,
    width: "100%",
  },
});

export default styles;
